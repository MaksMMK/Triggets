#     СОБЫТИЙНО-ОРИЕНТИРОВАННОЕ ПРОГРАММИРОВАНИЕ
import random

# Сообщение - блок данных определенной структуры, который используется для обмена информацией между объектами

#  Сообщение содержит сообщение об адресате тлт широковещателоьному, также содержит числовой код (тип) сообщения и параметры (дополнительные данные)

# Событие - переход какого- либо объекта из одного состояния в другое.

# Нажатие на клавишу
# Щелчок мышью
# Перемещение окна
# Поступление данных из сети
# Запрос к веб-серверу
# Завершение вычеслений
# ...



from simpletk import *
from tkinter.messagebox import *
from random import *


def askOnExit(event):
    if askokcancel("Подтверждение", "Вы точно хотите закрыть программу?"):  # Берем метод из tkinter.messagebox (сначала название вылазшего окна (сверху слева), потом (вопрос))
        app.destroy()     # Закрытие окна


def click(event):
    if showinfo("Подтверждение", "Вы щелкнули по форме"):   # Берем метод из tkinter.messagebox (сначала название вылазшего окна (сверху слева), потом (утверждение))
        pass


def Bcolor(event):
    # for i1 in "02478ACDF":
    #     for i2 in "0135679ACDF":
    #         for i3 in "0124589BDEF":
    #             for i4 in "012567ABDF":
    #                 for i5 in "012567ABDE":
    #                     for i6 in "023479ABCEF":
    #                         i = "#" + i1 + i2 + i3 + i4 + i5 + i6
    #                         a.append(i)
    a = ["#0000FF", "#008000", "#FF0000", "#00BFBF", "#BF00BF", "#BFBF00", "#000000"]
    app.background = (choice(a))


a = []
app = TApplication("Первая форма")  # Создаем окно, пишем название
app.position = (800, 400)           # Позиция, где вылезает окно
# Формы, размеры окна
app.size = (500, 200)               # Размер вылезающего окна
app.resizable = (True, True)       # Изменение по разимерам окна (если окно (False; False), то окно будет недвижимым)
app.minsize = (400, 200)           # Минимальное возможное разрешение окна
app.maxsize = (500, 300)           # Максимальное расширение окна
app.title = "Изменение название"   # Изменение названия окна

app.onCloseQuery = askOnExit       # Привязка окна к функции проверки закрытия    onCloseQuery - ЧИСТО ДЛЯ ЗАКРЫТИЯ (СОБЫТИЕ)
app.onClick = click                # Привязка окна к функции клика на окно    onClick - ЧТО БУДЕТ, ЕСЛИ ТАПНУТЬ НА ОКНО (СОБЫТИЕ)
app.onDblClick = Bcolor            # Привязка окна к функции смены цвета за два дабл клика    onDblClick  - ЧТО БУДЕТ, ЕСЛИ ТАПНУТЬ ДВАЖДЫ НА ОКНО (СОБЫТИЕ)


app.run()                           # Вывод окна

